export const ${Utils.upperCamel(entity.name)}ActionConstants = {
    SET_${Utils.upperCamel(entity.name)}S: 'SET_${Utils.upperCamel(entity.name)}S',
    ADD_${Utils.upperCamel(entity.name)}: 'ADD_${Utils.upperCamel(entity.name)}',
    UPDATE_${Utils.upperCamel(entity.name)}: 'UPDATE_${Utils.upperCamel(entity.name)}',
    DELETE_${Utils.upperCamel(entity.name)}: 'DELETE_${Utils.upperCamel(entity.name)}',
    STORE_${Utils.upperCamel(entity.name)}S: 'STORE_${Utils.upperCamel(entity.name)}S',
    FETCH_${Utils.upperCamel(entity.name)}S: 'FETCH_${Utils.upperCamel(entity.name)}S',
}